apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "management-tools.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "management-tools.name" . }}
    helm.sh/chart: {{ include "management-tools.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "management-tools.fullname" . }}:80']
  restartPolicy: Never
